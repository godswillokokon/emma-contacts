import {Dimensions} from 'react-native';

export const HEIGHT = Dimensions.get('screen').height;

export const WIDTH = Dimensions.get('screen').width / 3;

export const IMAGE_WIDTH = Dimensions.get('screen').width / 5.5;
